<article class="recipie">
  <div class="title">
    <a href="/dashboard/recipie/{{id}}">{{title}}</a>
    <span>({{comment}})</span>
  </div>
  <div class="meta">
    {{user.username}} on {{format_date createdAt}}
    |
    <a href="/recipie/{{id}}">{{comments.length}}</a>
  </div>
</article>